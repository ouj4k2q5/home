import { Box, Flex } from "@chakra-ui/react";

export const Header = () => {
  return (
    <header>
      <Box backgroundColor={"blackAlpha.300"}>
        <Flex h={16}></Flex>
      </Box>
    </header>
  );
};
